🚀 AI Text Processing Workflow using LangGraph

A simple multi-step AI text processing workflow built using **LangGraph**.

This project demonstrates how a complex text-processing task can be divided into multiple specialized nodes and connected through a **shared state**.

The workflow takes raw text from the user, corrects its grammar and spelling, improves the writing, and finally translates the polished content into French.
